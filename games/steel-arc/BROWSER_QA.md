# 双客户端浏览器验收（2026-09-23）

环境：本地服务器 127.0.0.1:18772，两个独立浏览器标签页，桌面视口 1280×800。通过页面按钮操作，无 AI 补位。

## 实测记录

- 创建房间、第二玩家输入房间码加入、准备、开始战斗：通过。
- 两端出现打靶模式提示：通过。
- A1 发射后炮弹可见，B1 获得操作权，地形和坦克没有消失：通过。
- 初次测试发现 B1 提交后立即切为山地，第二发仍在飞行：失败，已修复。
- 修复后重新加载两端并新建房间复测：校准场景保留到结算确认，两处落点出现爆炸烟尘。
- 两端出现分数结算文字：通过。本次默认参数两端均为 0 分，没有覆盖非零分数滚动效果；没有以自动计时断言验证精确 3 秒时长。
- 确认开始决斗后恢复山地：通过。
- B 队正式视角己方在左：截图确认。
- A、B 各完成一次正式射击，进入第 2 轮，地图保持渲染：通过。
- 两端浏览器 error 日志：均为空。

## 本轮修复

- 缓存独立的校准展示场景，不让服务端提前切换后的山地覆盖仍在播放的校准弹。
- 结算确认前阻止正式行动；确认后释放校准场景、重置镜头。
- 修正 B 队画布镜像位移中的重复缩放及相机偏移。
- 非镜像校准场景不再反转 B 队鼠标瞄准输入。

## 尚未通过或未覆盖

- 结算弹窗期间服务端正式回合倒计时仍在消耗，需要后续同步处理。
- 未完成整局击败对手和再来一局浏览器验收。
- 未测试 3～4 人房、真实跨机器网络、后台标签长时间暂停和断线重连。
- 不能据此宣称整个多人模式零缺陷。

辅助验证：引擎及渲染回归测试 40/40 通过；语法及 diff 检查通过。未提交或推送。
